{
  "name": "@guestpost/database",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc && mkdir -p dist/prisma && cp src/prisma/*.node dist/prisma/",
    "dev": "tsc --watch",
    "db:generate": "prisma generate && mkdir -p dist/prisma && cp src/prisma/*.node dist/prisma/ 2>/dev/null || true",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
